%h1
  %a{:href => "/"} Picasa Gallery
#content
  = @photos.pager.to_html("/")
  #thumbnails
    - @photos.each do |img|
      %a.thumbnail{:href => img['image'], :rel => "gallery"}
        %img{:src => img["thumb"], :alt => "Soshi Image"}/
  = @photos.pager.to_html("/")